const CustomError = require("../types/Error")

class CustomHeader {
    constructor(options) {
        this.build = boolean;
        this.library = "node-fetch" | "got";
        this.cookieContent = "";
        this.requestHeader = "";
    }
}
module.exports = CustomHeader